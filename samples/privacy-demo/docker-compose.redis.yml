services:
  redis:
    image: redis:7-alpine
    container_name: privacy-demo-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - privacy-demo-redis-data:/data

volumes:
  privacy-demo-redis-data:
